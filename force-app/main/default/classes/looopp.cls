public class looopp {
    integer x=1;
    public void math()
    {
        for(x=1;x<=10;x=x+1)
{
    system.debug('condition exist'+x);
}
while(x<=5)
        {
         system.debug('while loop exist'+x);
            x=x+1;
            break;
        }
       system.debug('while loop doesnt exist'+x);
}
    
}