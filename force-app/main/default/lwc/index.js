/*let day = 3;
let dayName;

switch (day) {
  case 1:
    dayName = 'Sunday';
    break;
  case 2:
    dayName = 'Monday';
    break;
  case 3:
    dayName = 'Tuesday';
    break;
  
  default:
    dayName = 'Invalid day';
}

console.log(dayName); // Tuesday

const { default: LastReferencedDate } = require("@salesforce/schema/Lead.LastReferencedDate")
const { default: LeadSource } = require("@salesforce/schema/Lead.LeadSource")
const { default: Phone } = require("@salesforce/schema/Lead.Phone")
const { default: Website } = require("@salesforce/schema/Lead.Website")

const Lead = ['Name', 'City', 'leadsource']
Let LeadSource = Website, Phone, LastReferencedDate

function f() {
  
}
class person 
{
   let x = [Name, Total, Result];

display(x)
{
  let result =(x.Result='Pass');
  if(result){
    console.log(this.Name);
    console.log(this.Total);
    console.log(this.Result);
  }
  else{
    console.log('fail')
  }
  
}
let obj1=new person(['Matthew', 400, 'Pass']);
let obj2=new person(['Vinoth', 200, 'Fail']);
let obj3=new person(['John', 300, 'Pass']);

*/
var a  = [
  {
    Name:'Sathish',
    City:'Bangalore',
    
  },
  {
    Name:'Micheal',
    City:'Bangalore',
    Phone: 7364783628,
  },
  {
    Name:'Carlos',
    City:'Chennai',
    Phone: 8873463792,
  },
  {
    Name:'Rahul',
    City:'Mysore',
    Phone: 9789189527,
  }
]

function greet(){
  if(a.LeadSource=Phone){
    let x = this.a.LeadSource.count();
    console.log(`The total lead source are ${x}`);
    return x;
    

  }
}

greet();

/*
let str = ' AJ Skill'
undefined
typeof str
'string'
str.indexOf('i',0)
6
str.trim().indexOf('l')
6
str.trim().indexOf('l',6)
6
str.trim().indexOf('S')
3
str.indexOf('S')
4
*/
